apiVersion: apps/v1
kind: Deployment
metadata:
  name: cryptpad
  namespace: cryptpad
spec:
  template:
    spec:
      containers:
      - name: cryptpad
        image: docker.io/cryptpad/cryptpad:version-5.5.0 # {"$imagepolicy": "cryptpad:cryptpad"}
